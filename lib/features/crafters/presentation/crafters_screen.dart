import 'package:flutter/material.dart';

class CraftersScreen extends StatelessWidget {
  const CraftersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Crafters screen scaffolded. Next: full-bleed parallax portfolio sections.'),
    );
  }
}
