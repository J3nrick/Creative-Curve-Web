import 'package:flutter/material.dart';

class ContactModalScreen extends StatelessWidget {
  const ContactModalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Contact screen scaffolded. Next: centered macOS-style blurred modal panel.'),
    );
  }
}
